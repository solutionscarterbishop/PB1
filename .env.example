# PB1 Environment Variables

# === REQUIRED ===
SUPABASE_URL=https://your-project.supabase.co
SUPABASE_SERVICE_ROLE_KEY=your-service-role-key

# === REQUIRED FOR INGEST FEATURES ===
APIFY_TOKEN=your-apify-api-token
INGEST_TOKEN=your-secret-ingest-token
APIFY_REVIEWS_DATASET_ID=your-reviews-dataset-id  # Required for apify-reviews ingest (no fallback)

# === OPTIONAL (have defaults) ===
PORT=10000
# APIFY_DATASET_ID=your-stores-dataset-id  # Only needed for /api/ingest/apify/pull

# === OPTIONAL READ API AUTH ===
# READ_API_TOKEN=your-read-api-token  # Optional: token for read API auth
# REQUIRE_READ_AUTH=false             # Set "true" in production to require read auth

# === OPTIONAL TABLE NAME OVERRIDES ===
# EXEC_WEEKLY_TABLE=exec_weekly
# STORES_TABLE=stores
# REVIEWS_TABLE=reviews
# INGEST_EVENTS_TABLE=ingest_events
# RPC_REFRESH_ROLLUPS=refresh_rollups
